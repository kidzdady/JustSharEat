/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  // Add other Next.js configurations here if needed in the future
  // For example, to enable experimental features or configure image optimization.
  // images: {
  //   domains: ['example.com'], // Add domains for external images
  // },
};

module.exports = nextConfig;